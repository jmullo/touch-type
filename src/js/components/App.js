import { TextArea } from 'components/TextArea';

export const App = () => {
    
    return (
        <TextArea text={"Hyväntahtoinen aurinko katseli heitä. Se ei missään tapauksessa ollut heille vihainen."} />
    );

};
